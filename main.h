#ifndef MAIN_H
#define MAIN_H

#include <stdarg.h>
#include <unistd.h>
#include <stdio.h>
/* these below are the print func */
int _putchar(char *c);
int _puts(char *str);


/* Declarations of .c files func*/
int _printf(const char *format, ...);

#endif
